source "https://rubygems.org"


gem "activesupport"
gem "awesome_print"
gem "pry-byebug"
# then enter bundle install in terminal after main %. i.e. main % bundle install
#this will create "Gemfile.lock" file on the left, don't manually edit this. this will list the version number of each of these gems, so that someone using your code can use the older version if needed. 


#finally, go back to your code file (hello.rb) and require them as normal. this just saves the terminal isntall part. 
